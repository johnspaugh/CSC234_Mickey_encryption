#include <iostream>
#include <fstream>
#include <string> 
#include "../include/AddPieToEnd.h" 
// #include <AddPieToEnd.h>
#include "huffman.h"

using namespace std;

int main() {
    Node* root;
    std::string code;
    // std::string>& codes;
    // std::map<unsigned char;
    // generateCodes( root, code, std::map<unsigned char,  codes);

    return 0;
}